services:
  - type: web
    name: n8n-docker
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: N8N_BASIC_AUTH_ACTIVE
        value: "true"
      - key: N8N_BASIC_AUTH_USER
        value: "admin"
      - key: N8N_BASIC_AUTH_PASSWORD
        value: "admin518n518"

      - key: N8N_API_KEY_AUTH_ACTIVE
        value: "true"

      # PostgreSQL (Supabase) - Final Solution using separate variables for robustness
      - key: DB_TYPE
        value: "postgresdb"
      - key: DB_POSTGRESDB_URL
        value: "postgresql://postgres.bypuaavvyvvkknbcyfnp:Ksa518n123Ksa518n@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

      - key: N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS
        value: "true"

      - key: N8N_ENCRYPTION_KEY
        generateValue: true
      - key: N8N_HOST
        value: "0.0.0.0"
      - key: N8N_PORT
        value: "5678"

      - key: WEBHOOK_URL
        value: "${{ env.RENDER_EXTERNAL_URL }}"
      - key: N8N_TRUST_PROXY
        value: "true"
